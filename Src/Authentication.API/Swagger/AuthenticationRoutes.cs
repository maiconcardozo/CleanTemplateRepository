using Authentication.API.Resource;

namespace Authentication.API.Swagger
{
    internal static class AuthenticationRoutes
    {
        public const string GenerateToken = nameof(ResourceRoutesAPI.GenerateToken);
    }
}
